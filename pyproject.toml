[project]
name = "corridor-bot"
version = "1.0.0"
description = "Anonymous-message Telegram bot running on Render"
requires-python = ">=3.9"
dependencies = [
    "aiogram>=3.7,<4",
    "redis>=5.0,<6",
    "jdatetime>=5.0,<6",
    "flask>=3.0,<4",
    "apscheduler>=3.10,<4",
]
